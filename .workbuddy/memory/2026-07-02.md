# 2026-07-02 工作日志

## bluebell 项目依赖安装与诊断
- 运行 `go mod tidy`：首次失败，根因是 `github.com/swaggo/gin-swagger` 从 v1.6.0 起将 `swaggerFiles` 子包拆到独立仓库 `github.com/swaggo/files`，旧导入路径 `gin-swagger/swaggerFiles` 在 v1.6.1 已不存在。
- 修复：router/router.go 导入由 `"github.com/swaggo/gin-swagger/swaggerFiles"` 改为 `swaggerFiles "github.com/swaggo/files"`，调用处 `swaggerFiles.Handler` 无需改动。
- 修复后 `go mod tidy` 成功，go.mod 写入 12 个直接依赖，go.sum 已生成（约 26KB）。
- 直接依赖：gin v1.12.0、gin-contrib/pprof v1.5.4、go-redis/redis v6.15.9+incompatible、go-sql-driver/mysql v1.10.0、jmoiron/sqlx v1.4.0、natefinch/lumberjack v2.0.0+incompatible、spf13/viper v1.21.0、swaggo/files v1.0.1、swaggo/gin-swagger v1.6.1、fsnotify/fsnotify v1.10.1、go.uber.org/zap v1.28.0。
- 环境：go1.26.4 windows/386，GOPROXY=goproxy.cn。

## 仍存在的编译阻塞问题（项目代码不完整，非依赖问题）
- `bluebell/controller`：目录存在但为空（无 .go 文件），router.go 却调用了 9 个 handler（SignUpHandler/LoginHandler/GetPostListHandler2/GetPostListHandler/CommunityHandler/CommunityDetailHandler/GetPostDetailHandler/CreatePostHandler/PostVoteController）。
- `bluebell/middlewares`：目录不存在，router.go 调用 middlewares.JWTAuthMiddleware()。
- `bluebell/docs`：目录不存在，router.go 用 `_ "bluebell/docs"` 导入（swag 生成的文档包，需 `swag init` 生成）。
- `templates/index.html` 与 `static/`：均不存在，router.go 运行时 LoadHTMLFiles/Static 会 panic。
- 结论：基础设施层（main/router/logger/dao/setting）已搭好，业务层（controller/middlewares/models/logic）及 swag docs、前端模板尚未实现，属半成品项目。
